<div class="container">
  <div class="jumbotron">
    <h1>Repositories#index</h1>
    <p>Find me in app/views/repositories/index.html.erb</p>
  </div>
  
  <% @respositories.each do |r| %>
    <div class="row">
      <p><%= r.url %> hosted at <%= r.host %></p>
    </div>
  <% end %>
</div>